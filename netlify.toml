[build]
  publish = "."
  command = "npm install && node scripts/generate-config.js"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/"
  to = "/Peekd.html"
  status = 200

[build.environment]
  NODE_VERSION = "20"
